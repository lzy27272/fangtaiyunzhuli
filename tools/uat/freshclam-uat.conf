# Minimal one-shot UAT updater configuration. The database and certificate
# directories are supplied explicitly on the command line by the runbook.
DatabaseMirror database.clamav.net
Checks 24
TestDatabases yes
