# TECH-V0.2发布阻塞交接单

日期：2026-07-18  
当前正式版本：`TECH-V0.1`  
当前候选：`TECH-V0.2-rc.3-local`  
当前结论：`NO-GO / TECH-V0.2 Unreleased / Sprint 3未启动`

## 已在本地关闭

- 六角色和三条业务闭环技术UAT通过。
- 后台Worker、账号/任职生命周期、租户隔离和一人多岗回归通过。
- Live UAT宿主停机顺序修复并完成真实复验。
- 发布JAR数据库密码回退值已移除；缺少外部密码即失败。
- 48项后端回归零失败；Flyway V1—V13通过。
- RC3两次独立构建的5项制品一致；本地加固制品指纹为 `546fc5175d97af2e0bbe3736468b1366d8890e89a6c6a6d761db4d40eba089ee`。
- 发布包深度扫描0命中、0错误。
- 外部证据包校验器已失败关闭普通JSON伪签署、重复身份、重解析点路径和哈希漂移，并强制SSO/运维各10方专项审批、原始照片字节绑定、受控Git fetch/push远程、HEAD身份以及绑定精确远程/分支/HEAD/RC标签/manifest/发布时间的仓库责任人审批。

## 仍需外部真实输入的5项门禁

| 门禁 | 当前事实 | 必须由谁提供 | 关闭证据 |
|---|---|---|---|
| REL-P0-02 目标企业SSO | 只有本地受控OIDC；未接目标IdP | 集团IT/身份管理员 | 目标issuer/audience/claims配置；六角色登录；账号停用、任职失效、退出失效；受控签署平台或证书验证记录 |
| REL-P0-03 10方签署 | 0/10 | 产品、技术、数据安全、运维、业务及指定验收代表 | 10份独立受控签署导出；唯一签署人/签名ID；验证ID、时间及本地验证证据 |
| REL-P0-04 现场照片与生产附件链 | 当前为1×1测试PNG和本地文件存储 | 试点门店、业务验收人、存储/安全负责人 | 真实现场照片来源；目标对象存储上传下载；恶意文件拒绝；备份恢复；业务/安全签署 |
| REL-P0-05 源码与制品追溯 | `main`无HEAD；无Git身份、远程、RC标签；manifest的`source.commit=null` | 仓库管理员/发布负责人 | 经确认的Git身份与远程；干净提交；注解`TECH-V0.2-rc.3`标签；同一提交重建manifest；tag/HEAD/制品一致 |
| REL-P0-06 目标环境运行保障 | 只有本地恢复演练 | 运维、DBA、安全负责人 | 目标PostgreSQL部署；备份保留与加密；恢复/回滚；监控告警；附件备份；运维签署 |

另外，环境责任人必须确认旧数据库密码回退值从未在任何环境使用；如使用过，先轮换，再提交受控轮换记录。

## 当前Git事实

- 分支：`main`。
- 有效HEAD：无。
- `user.name`：未配置。
- `user.email`：未配置。
- remote：0。
- 工作区全部尚未进入首个受控提交。

因此不能由工具伪造提交身份、远程仓库或发布标签。仓库管理员确认身份、远程和提交范围后，才可执行首个提交与注解RC标签；标签之后必须基于该HEAD重新构建RC3制品，不能沿用`source.commit=null`的本地制品。

## 外部证据提交方式

1. 将`TECH-V0.2-EXTERNAL-EVIDENCE-BUNDLE.example.json`的结构复制到已被Git排除的`.uat-runtime/release/TECH-V0.2-EXTERNAL-EVIDENCE-BUNDLE.json`；禁止把真实签署人和目标环境元数据放入`docs/releases`。
2. 按模板把目标SSO、目标运维、现场附件和10方独立签署的受控导出文件放入`.uat-runtime/release/external-evidence/`；所有文件必须位于工作区真实目录，不能经过symlink或junction。
3. 仅运行唯一正式入口`Invoke-TechV02ReleaseClosure.ps1`，不得手工串联或单独运行三个内部子工具作为发布授权。总控会依次执行本地证据一致性、门禁输入生成、外部证据独立重算和最终6项门禁；任何阶段失败都会停止后续阶段。
4. 只有总控输出`READY_FOR_RELEASE_APPROVAL`、最终门禁`6 PASS / 0 BLOCKED`、最终门禁返回的锁定读取SHA等于已验证输入SHA，且门禁输入在重算及最终门禁前后均未漂移，才允许进入GO人工审批。

详细格式见`TECH-V0.2-EXTERNAL-EVIDENCE-BUNDLE.md`。本地工具不会创建签字、批准、Git提交、标签或外部平台记录。

当前总控实跑：本地证据一致性`15/15 PASS`；正式外部证据包不存在，因此停在`EXTERNAL-EVIDENCE-GENERATION`，后续重算与最终门禁均未运行，规范门禁输入文件写入数为0。早期`external-audit`文件经审计仅为全部字段`PENDING`且引用`.example.json`的负向测试派生物，已保留到`.uat-runtime/release-tests`并明确标记`invalid-template-derived`，不具备发布证据效力。

收口实现稳定快照复验为PASS：契约测试`19/19`、总控负向测试`6/6`、外部证据主体绑定与负向测试`34/34`、最终门禁证据绑定测试`24/24`、证据一致性`15/15`。覆盖测试旁路拒绝、严格schema/版本/RC标签、ISO 8601显式时区、锁定消费SHA、Git引用指纹、10方专项审批、原图字节与篡改拒绝、仓库审批作用域、五类正式制品唯一性、Worker/manifest/SHA256SUMS末端重验、manifest瞬时A→B→A替换拒绝、UTF-8 BOM兼容、已有/新建目标原子晋升、验证异常清理、替换后清理失败回滚及回滚失败保留唯一备份。测试后临时文件、签署、审批、提交、标签、发布状态修改和网络写入均为0。该结论只关闭本地发布工具缺陷，不替代下列真实SSO、真人签署、现场附件、Git追溯和目标运维证据。

## 通过后的执行顺序

```text
外部证据校验 PASS
→ 发布门禁 6/6 PASS
→ 发布责任人批准 TECH-V0.2
→ 创建正式版本记录并标记 Released
→ TECH-V0.3技术冻结生效
→ 启动Sprint 3编码
```
