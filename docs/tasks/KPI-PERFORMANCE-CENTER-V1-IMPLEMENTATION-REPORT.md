# KPI 绩效考核中心 V1 实施与本地部署报告

日期：2026-08-12
状态：本地 Pilot 已部署；真实 PMS/OTA/Cookie 与外部通知未启用

## 已交付范围

- 行政人事部 KPI 专用中心：岗位模板、考核关系、四周过程考核、月度考核、复核/异议/更正、奖金结算与一键导出。
- 首批岗位字典：集团副总、OTA运营经理、OTA运营助理、门店店长、店助、前厅主管、前台员工、客房部负责人、客房服务员。
- OTA运营经理官方模板：经营结果20分、差店整改35分、每日巡检25分、公司任务20分；岗位专属奖金加减独立配置。
- 每月固定4周：1–7日、8–14日、15–21日、22日至月末；比例型指标每周保持同一目标，加总型指标才按25%分解。
- 多渠道独立判定：每个已配置渠道分别进行三项前30%及巡检留痕，不允许渠道平均或抵消。
- 巡检留痕：早/中/睡前3时段，服务端账号、姓名和时间签署；原记录只读，只能追加更正。
- 异常闭环：普通异常30/60分钟/4小时，重大异常15/30分钟/2小时；异常本身不扣分，漏检、虚假正常与超时才生成扣分事实。
- 模板可配置与Excel导入：导入只生成草稿，仍须审核、发布与未来周期生效。
- 审计与防篡改：事实、人工评分、证据、修订、异议、更正和导出均留痕；已发布模板不可覆盖修改。

## 数据库与应用部署证据

- Flyway：从 V23 成功迁移至 V27，日志显示4个迁移一次成功。
- KPI数据结构：31张 `kpi_*` 表、25项 `kpi.*` 权限、1套已发布OTA运营经理模板、3个启用巡检时段。
- 本地运行：Core API `http://127.0.0.1:18080` 健康为 `UP`；Web `http://127.0.0.1:4180` 返回200。
- 登录门禁：前端按Pilot `bearer`模式构建，未认证访问`/api/v1/iam/me`返回401；登录后显示显式“退出”按钮，退出清除本地令牌并返回登录页。
- 防误部署：入口写入认证模式元数据；本地Pilot启动脚本拒绝认证模式不是`bearer`的Web构建，避免再次自动落入默认验收岗位。
- 前端Pilot资源：`KpiRoutes-CPSyj5Ux.js` 返回200，大小26,125字节。
- 受保护CEO测试账号只读冒烟：登录、模板列表、考核周期、巡检时段全部通过；官方OTA模板可见，巡检检查项按数组返回。
- 一键导出冒烟：`scorecards.csv` 返回200、UTF-8 BOM、附件下载响应头正确。

## 自动化验证

- 完整后端回归：88项，0失败、0错误、2项按运行条件跳过；共生成33份测试报告。
- 关键后端测试：10项，0失败（KPI计分、Flyway全迁移、运行时API、日报回归）。
- Flyway空库集成测试：27个迁移全部通过，RLS与不可篡改触发器通过。
- 前端生产构建：TypeScript与Vite构建通过，63个模块转换成功。
- 当前部署JAR：`hotel-ai-os-core-api-0.2.0-pilot.7.jar`，SHA-256：`4003D34BF4AAB0AEFFB1B23752B38187550374D014595BC387429C7F544AF9FD`。
- 部署前逻辑备份：95个文件，约3.73MB；manifest SHA-256：`0050CA699B3DE6AA38E66C17ACDE43E159976CB1FA223242D46E8AED2F464989`。
- 部署证据目录：`.uat-runtime/pilot/kpi-deploy-20260812-125113/`，当前共111个文件、38,379,043字节。

## 当前边界

- 未接入或读取任何真实 PMS、OTA Cookie、渠道账号或预订个人信息。
- 未发送企微、短信、邮件或其他真实外部通知；目前仅保留站内通知模型与读回执。
- 当前部署为本地 Pilot，不代表正式生产发布或公网连接器授权。
- 其他首批岗位尚未预置官方模板，由行政人事后台自行配置，或后续导入用户提供的考核表。

## 回滚依据

- 部署前旧可执行 JAR 已保存为 `hotel-ai-os-core-api-rollback.jar`。
- 数据库回滚采用部署前逻辑备份恢复；不得用删除V25–V27表的方式回退。
- 已发布KPI事实与审计数据保持追加式，禁止直接覆盖。
